CREATE SCHEMA `PE2_DAW`;


CREATE TABLE `PE2_DAW`.`USUARIO` 
(
  `ID` INT NOT NULL,
  `USER` VARCHAR(45) NOT NULL,
  `PASS` VARCHAR(45) NOT NULL,
  `LAST_LOGIN` DATETIME NOT NULL,
  PRIMARY KEY (`ID`)
);
